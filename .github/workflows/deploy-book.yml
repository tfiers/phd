name: deploy-book-workflow

# Only run when anything on the master branch, in the `notebooks` directory, changes
on:
  push:
    branches:
    - master
    - notebooks/**

# Install dependencies, build the book, and push it to `gh-pages`
jobs:
  deploy-book-job:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2

    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Cache pip install
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ hashFiles('notebooks/requirements.txt') }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install -r notebooks/requirements.txt

    - name: Generate & process table of contents, and build the book
      working-directory: notebooks
      run: python build_website.py

    - name: Push the book's HTML to gh-pages branch
      uses: peaceiris/actions-gh-pages@v3.6.1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: notebooks/_build/html
